{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug cui",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/Debug/cui",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake Build (Debug)"
        }
    ]
}

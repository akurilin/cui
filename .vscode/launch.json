{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug hello",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/Debug/hello",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake Build (Debug)"
        }
    ]
}
